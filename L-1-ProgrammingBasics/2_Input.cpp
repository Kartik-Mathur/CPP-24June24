#include <iostream>
using namespace std;

int main() {

	// To take input two things are required:
	// 1. Create a bucket
	// 2. Input the value from user in that bucket
	int a;
	// cin >> a; // We are taking user input and storing it inside bucket a

	cout << a;

	return 0;
}